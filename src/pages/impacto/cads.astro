---
import BaseLayout from "../../layouts/baselayout.astro";

const docs = [
  {
    title: "Artemis",
    description:
      "CAD completo del robot 2026 desarrollado por ARIA STEAMEX.",
    image: "/assets/artemis-cad.png",
    pdf: "https://cad.onshape.com/",
    external: true,
  },
];
---

<BaseLayout title="CADs | ARIA STEAMEX">

  <main class="min-h-screen bg-[#140019] text-white">

    <!-- HERO -->
    <section class="relative border-b border-white/10 overflow-hidden">

      <div class="absolute inset-0 bg-gradient-to-b from-[#2A0836] to-transparent opacity-60"></div>

      <div class="container-site py-24 relative z-10">

        <p class="text-sm tracking-[0.3em] uppercase text-white/40 mb-6">
          Impacto / CADs
        </p>

        <h1 class="text-5xl md:text-7xl font-black mb-8">
          CADs
        </h1>

        <p class="max-w-3xl text-lg text-white/65 leading-relaxed">
          Diseños mecánicos, ensamblajes y documentación CAD
          desarrollada por el equipo durante la temporada.
        </p>

      </div>

    </section>

    <!-- GRID -->
    <section class="container-site py-20">

      <div class="grid md:grid-cols-2 xl:grid-cols-3 gap-8">

        {
          docs.map((doc) => (
            <div class="group border border-white/10 rounded-[28px] overflow-hidden bg-white/5 opacity-70 cursor-not-allowed">

              <div class="aspect-[4/3] overflow-hidden bg-[#2A0836]">

                <img
                  src={doc.image}
                  alt={doc.title}
                  class="w-full h-full object-cover grayscale"
                />

              </div>

              <div class="p-8 flex flex-col justify-between min-h-[240px]">

                <div>

                  <h2 class="text-3xl font-black mb-4">
                    {doc.title}
                  </h2>

                  <p class="text-white/60 leading-relaxed">
                    {doc.description}
                  </p>

                </div>

                <div class="flex items-center justify-between mt-10">

                  <span class="text-white/50">
                    Próximamente
                  </span>

                </div>

              </div>

            </div>
          ))
        }

      </div>

    </section>

  </main>

</BaseLayout>